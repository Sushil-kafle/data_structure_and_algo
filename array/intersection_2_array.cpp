#include <bits/stdc++.h>
using namespace std;

int main()
{

    int size_a = 5;
    int size_b = 5;

    vector<int> a(size_a);
    vector<int> b(size_b);

    for (int &i : a)
    {
        cin >> i;
    }

    for (int &i : b)
    {
        cin >> i;
    }

        vector<int> ans;

    return 0;
}